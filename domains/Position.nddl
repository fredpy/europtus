
#include "europtus.nddl"

class PosState extends Timeline {
      predicate Position {
      	float latitude, longitude;
	float depth, height, z;

	duration == 1;
      }

      PosState() {
      }
}
